server {
    listen 80;
    server_name raduga.miraclethings.nl;

    location /photos/ {
        alias {{ upload_folder }};
        autoindex off;
    }

    location / {
        include uwsgi_params;
        uwsgi_pass unix:/tmp/raduga.sock;
    }
}
